[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    auth: tests for authentication endpoints
    users: tests for user endpoints  
    tasks: tests for task endpoints